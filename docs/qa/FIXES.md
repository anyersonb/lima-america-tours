# Fixes aplicados durante QA (modo autonomía) — Lima América Tours

Cada fix cumple la verificación de 3 partes: (a) test que falla→pasa o evidencia en navegador; (b) suite de humo verde; (c) esta línea.

| # | Rama / commit | Qué estaba roto | Causa raíz | Cambio | Prueba |
|---|---|---|---|---|---|
| 1 | `qa/setup` | 5 tests daban 500 (CartTest ×2, CheckoutTest·thanks, NewsletterTest ×2). Además **fragilidad en TODAS las páginas** (layout) si falta cualquier `setting` de red social. | `resources/views/components/jsonld.blade.php:34-37` accedía `$settings['social_x']` por corchete → `Undefined array key` cuando `settings` está vacío/incompleto. Las líneas 38-40 ya usaban `?? null`. | `?:` → `?? null` en las 4 líneas de redes sociales. | `php artisan test`: **9→4 failed, 38 passed (133 assertions)**; `SmokeTest` verde; CartTest/NewsletterTest/CheckoutTest·thanks pasan. Baseline actualizada a 4. |
| 2 | `qa/panel-filament` | Hallazgo #1 (panel-filament.md): campos del formulario Crear/Editar de Region/Category/Testimonial/Offer/NewsletterSubscriber/ContactLead en inglés ("Is active", "Order", "Seo title"…). El fix `81ba722` solo tradujo columnas de tabla. | Los `TextInput`/`Toggle`/`FileUpload`/`DateTimePicker` del `form()` de esos 6 Resources nunca llamaban a `->label()`; Filament genera el label por defecto (en inglés) a partir del nombre de columna. | Se agregó `->label('...')` en español a cada campo sin label de los 6 `form()` (`RegionResource`, `CategoryResource`, `TestimonialResource`, `OfferResource`, `NewsletterSubscriberResource`, `ContactLeadResource`), reusando el texto ya usado en `table()` para consistencia. | `tests/Feature/PanelFormLabelsSpanishTest.php` (6 tests): fallan con 403 sin el email de admin correcto, luego fallan con el texto en inglés presente si se revierte el Resource (confirmado con `git stash`); pasan con el fix. |
| 3 | `qa/panel-filament` | Hallazgo #6: columna "Bloqueo" de `BlockedDateResource` mostraba "Todos los Luness", "Todos los Martess" (doble plural + mayúscula) para 5 de 7 días. | `"Todos los {$day}s"` concatenaba una "s" a nombres de día ya capitalizados y ya invariantes en plural en español (lunes/martes/miércoles/jueves/viernes), en vez de usar la forma plural correcta. | Se extrajo la lógica a `BlockedDateResource::weeklyBlockLabel(?int $weekday): string` con un mapa explícito de plurales correctos en minúscula (domingos/lunes/martes/miércoles/jueves/viernes/sábados), testeable sin Livewire. | `tests/Unit/BlockedDateWeeklyLabelTest.php` (9 tests): fallan con `BadMethodCallException` (método no existía) al revertir el archivo con `git stash`; pasan con el fix, para los 7 días + caso de fallback. |
| 4 | `qa/panel-filament` | Hallazgo #5: la pestaña "Contenido de la página" de `PageResource` (condicionada al `slug`) no aparecía en modo Crear al escribir `contacto`/`nosotros` — solo tras Guardar y reabrir en Editar. | El `TextInput` de `slug` no tenía `->live()`, así que Livewire no revalidaba los `visible(fn (Get $get) => ...)` de la pestaña mientras el editor escribía. | Se agregó `->live()` al campo `slug` en `PageResource::form()`. | `tests/Feature/PageResourceSlugLiveTest.php`: falla (`assertTrue` sobre `isLive()`) al revertir con `git stash`; pasa con el fix. |
| 5 | `qa/panel-filament` | Hallazgo #7: el `<title>` de la ficha de tour rompía mayúsculas intencionales — "QA_ Tour Prueba ñÑ áéíóú" se convertía en "Qa_ Tour Prueba Ññ Áéíóú". | `resources/views/tours/show.blade.php` recalculaba `$titleDisplay` con `mb_strtolower()` + `mb_convert_case(..., MB_CASE_TITLE)` sobre el título, en vez de usar el texto tal cual lo guardó el editor. | Se eliminó la transformación; `$titleDisplay = $tour->title` directamente (el accessor del modelo ya resuelve el idioma, sin recapitalizar). | `tests/Feature/TourShowTitlePreservesCasingTest.php`: falla (ve "Qa_ Tour Prueba" en el `<title>`) al revertir el blade con `git stash`; pasa con el fix. |
| 6 | `qa/panel-filament` | Hallazgo #2: el recurso "Ofertas" (`OfferResource`) no tenía ningún consumidor en el front pese a que `HomeController::fetchOffers()` ya calculaba `$offers` (activas, sin vencer, orden manual, límite 3) y se la pasaba a `home.blade.php` sin usarla. | La vista `home.blade.php` nunca leía la variable `$offers` — el pipeline de datos ya existía, solo faltaba la sección de presentación (wiring trivial, sin necesidad de diseño nuevo). | Se agregó una sección "Ofertas especiales" en `home.blade.php` (reutiliza las clases `.lat-dcard` de Tours Destacados: imagen, título, descripción, precio, CTA con link a `cta_url` o al tour vinculado) + `with('tour')` en `HomeController::fetchOffers()` para evitar N+1. | `tests/Feature/HomeOffersSectionTest.php` (4 tests: oferta activa se ve, inactiva no, vencida no, home sin ofertas no rompe): el primero falla al revertir blade+controller con `git stash`; los 4 pasan con el fix. |
| 7 | `qa/ficha-tour` | Hallazgo #4 (`ficha-tour.md`, F2): la ficha de tour generaba `<title>`/meta description/OG/keywords genéricos, ignorando `seo_title`/`seo_description`/`seo_image`/`seo_keywords` del `Tour` pese a que el CMS ya los guarda (`TourResource` tab "SEO"). Cada ficha compartía los metadatos sociales del sitio. | `TourController@show` nunca resolvía ni pasaba esos campos a la vista; `layouts/app.blade.php` solo leía `@section('title'/'description')`/`og_image` y el `seo_default_keywords` global, sin ninguna variable por-tour que priorizar. | `TourController@show` calcula `$seoTitle`/`$seoDescription`/`$seoImage`/`$seoKeywords` (con fallback a título/descripción/portada del propio tour) y los pasa a la vista; `layouts/app.blade.php` los prioriza sobre el `@section` y el `seo_default_keywords` cuando llegan. **No se tocó `tours/show.blade.php`** (el `@extends` comparte las variables del controlador con el layout automáticamente). | `tests/Feature/TourShowSeoMetaTest.php` (2 tests): con `git stash` del controlador+layout, 1 de los 2 falla (`<title>` con `seo_title` no aparece, ve el genérico); ambos pasan con el fix. |
| 8 | `qa/ficha-tour` | Hallazgo #7: al borrar un Tour desde el admin (soft delete estándar de Filament), `Testimonial` y `BlockedDate` ligados por `tour_id` quedaban huérfanos — la fila seguía en la BD apuntando a un `tour_id` que `Tour::find()` ya no resuelve (excluido por el scope global de `SoftDeletes`). Confirmado y purgado a mano en QA. | Las FK de `testimonials.tour_id`/`blocked_dates.tour_id` son `nullOnDelete()`, que solo actúa ante un `DELETE` real en la tabla `tours`; `Tour::delete()` (soft delete) solo hace un `UPDATE deleted_at`, así que ese constraint nunca se dispara y los hijos quedan intactos apuntando a un padre invisible. | `Tour::booted()` ahora escucha `static::deleting` y borra en cascada `$tour->testimonials()`/`$tour->blockedDates()` (ninguno de los dos tiene soft deletes propios), cubriendo tanto `delete()` como `forceDelete()`. Se agregó la relación `Tour::blockedDates()` y las factories `TestimonialFactory`/`BlockedDateFactory` (con `HasFactory` en `BlockedDate`). | `tests/Feature/TourDeleteCascadeTest.php` (2 tests: soft delete y forceDelete): con `git stash` del modelo, ambos fallan (`assertDatabaseMissing` no se cumple, las filas siguen); pasan con el fix. |
| 9 | `qa/ficha-tour` | Hallazgo #8: el formulario de Tour permitía guardar `price = 0` sin validación mínima; combinado con `price_before` > 0 producía un badge "Oferta especial −100%" engañoso, y el propio "Vista previa del descuento" del admin mostraba el copy incorrecto "Sin oferta activa (precio antes vacío)" para ese caso (la causa real era `price=0`, no `price_before` vacío). | El cálculo de descuento estaba duplicado (Placeholder del admin + `IconColumn`/tooltip de la tabla), sin un guard explícito de `price > 0`, y el campo `price` no tenía mínimo en la validación de Filament. | Se centralizó el cálculo en `Tour::offerDiscountPercent()` (+ `hasActiveOffer()`/`discountPercent()`), que exige `price > 0` **y** `price_before > price`; se reusa en el Placeholder (con un mensaje específico para `price <= 0`) y en la columna/tooltip "Oferta" de la tabla. Se agregó `->minValue(0.01)` al campo `price` en `TourResource::form()`. | `tests/Unit/TourOfferDiscountTest.php` (4 tests, sin BD): con `git stash` del modelo, la clase `Tour::offerDiscountPercent()` no existe y las 4 pruebas fallan (`Error: Call to undefined method`); las 4 pasan con el fix. |
| 10 | `qa/ficha-tour` | Hallazgo #1 (fila F2 #1): la ficha nunca mostraba el mensaje de fecha bloqueada (`$errors` no se leía) y el `<input type="date">` no daba ninguna señal de que una fecha estuviera bloqueada — el visitante veía el botón "Reservar ahora" como si no hiciera nada. | `resources/views/tours/show.blade.php` no tenía ningún `@if($errors->any())`/`@error` en la caja de reserva, y el `<input id="bkDate">` solo traía `min` (mañana), sin usar `blockedDates`/`blockedWeekdays` que `TourController@show` ya pasaba a la vista. Un `<input type="date">` nativo no tiene API para deshabilitar fechas/días sueltos. | Se agregó un bloque `@if($errors->any())` (`.lat-book-error`) al inicio del `<form>` para el round-trip del servidor, y validación JS en vivo (`input`/`change`/`submit`) que compara el valor elegido contra `@json($blockedDates)`/`@json($blockedWeekdays)`: si está bloqueada, marca el campo (`.lat-book-field.is-invalid`), muestra `#bkDateError` con el texto de `lang/es/booking.php` y bloquea el envío (`setCustomValidity` + `preventDefault`). No se tocó `TourController`/`BlockedDate`. | `tests/Feature/TourShowFichaFixesTest.php` (`test_tour_show_displays_error_after_booking_a_blocked_date`, `test_tour_show_exposes_blocked_dates_and_weekdays_for_the_datepicker`) + evidencia de navegador (Playwright contra `:8002`/`lima_america_qa`): mensaje visible tras el rechazo del servidor y campo marcado en rojo con mensaje inline al elegir la fecha bloqueada en vivo. |
| 11 | `qa/ficha-tour` | Hallazgo #2 (fila F2 #2): `<x-tour-comparison>` existía y `Tour::comparisonData()` ya resolvía los datos, pero la ficha nunca invocaba el componente — el bloque comparativo convencional/premium jamás aparecía aunque el CMS lo tuviera activo con datos. | `resources/views/tours/show.blade.php` nunca renderizaba `<x-tour-comparison>`. | Se agregó `$comparisonData = $tour->comparisonData()` y `@if($comparisonData) <x-tour-comparison :data="$comparisonData" variant="d" /> @endif` dentro de `.lat-detail-main`, envuelto en `.lat-detail-comparison` (solo espaciado). El propio componente ya valida internamente `enabled` + datos no vacíos. | `tests/Feature/TourShowFichaFixesTest.php` (`test_tour_show_renders_comparison_block_when_active_with_data`, `test_tour_show_hides_comparison_block_when_disabled`) + evidencia de navegador: tour `QA_` con comparativa activa la muestra completa (columnas, VS, frase final); con `enabled=false` no aparece rastro de los textos. |
| 12 | `qa/ficha-tour` | Hallazgo #3 (fila F2 #3): `$tourReviews` (testimonios aprobados del tour) ya llegaba desde `TourController@show`, pero la ficha nunca la pintaba — ninguna reseña individual de cliente era visible. | `resources/views/tours/show.blade.php` no tenía ninguna sección que iterara `$tourReviews`. | Se agregó la sección `.lat-reviews` (autor, estrellas 1-5 según `rating` del testimonio, fecha, comentario), visible solo si `$tourReviews` no está vacía. **No se agregó el formulario de envío de reseña** (decisión de negocio pendiente, ver `docs/qa/BACKLOG-CONTENIDO.md`). | `tests/Feature/TourShowFichaFixesTest.php` (`test_tour_show_renders_approved_testimonials`, `test_tour_show_hides_unapproved_testimonials`) + evidencia de navegador: reseña `QA_` aprobada visible con nombre/estrellas/fecha/comentario. |
| 13 | `qa/ficha-tour` | Hallazgo #4/#5 (fila F2 #5): `badge_text`/`badge_type` ya se usaban en el listado (`tours/index.blade.php`) pero la ficha los ignoraba por completo; solo mostraba el badge de oferta automático. | `resources/views/tours/show.blade.php` no leía `$tour->badge_text`/`badge_type`. | Se agregó un chip `.lat-detail-badge` (mismo mapeo de color que el listado: `success`→verde, `warn`→naranja, `error`/default→rojo) junto al título/rating, visible solo si `badge_text` no está vacío. | `tests/Feature/TourShowFichaFixesTest.php` (`test_tour_show_renders_badge_when_tour_has_one`, `test_tour_show_omits_badge_when_tour_has_none`) + evidencia de navegador: chip verde "QA_ CUPOS LIMITADOS" visible en `.lat-detail-rate`. |
| 14 | `qa/ficha-tour` | Hallazgo #9 (fila F2 #9): con un título extremo el breadcrumb de la ficha no truncaba y se veía pesado en una sola línea (no rompía el layout, pero era cosmético). | `.lat-crumb` no era flex y el `<b>` del título actual no tenía ningún límite de ancho/`text-overflow`. | `.lat-crumb` pasó a `display:flex` con `min-width:0`; el tramo del título actual (`.lat-crumb__current`) es `flex:1 1 auto` con `overflow:hidden;text-overflow:ellipsis;white-space:nowrap` y atributo `title` con el texto completo (tooltip nativo); Home/Tours quedan `flex:none` y siempre legibles. | `tests/Feature/TourShowFichaFixesTest.php` (`test_tour_show_breadcrumb_has_truncation_markup_for_long_titles`) + evidencia de navegador: en 375px el breadcrumb trunca con elipsis (`scrollWidth` 798px vs. `renderedWidth` 232px) sin overflow horizontal de página; en 1440px tampoco desborda. |
| 15 | `qa/paginas` | Hallazgo #3 (`panel-filament.md`): `contact.blade.php` extraía `$b = $page->blocks` pero nunca lo volvía a usar — el eyebrow/H1/lead del hero de Contacto estaban hardcodeados, ignorando los 9 campos de texto que el admin sí guarda (`hero_eyebrow`/`hero_title`/`hero_lead` ×3 idiomas). | Faltaba el helper `$bl()` (ya usado en `about.blade.php`) para leer `$b[key_locale]` con fallback; los 3 literales del hero nunca se reemplazaron por esa lectura. | Se agregó `$bl(key, fallback)` en `contact.blade.php` (mismo patrón que `about.blade.php`) y se sustituyeron `$L('ESTAMOS PARA AYUDARTE',...)`/`__('ui.contact_us')`/`$L('¿Tienes dudas...',...)` por `$bl('hero_eyebrow', ...)`/`$bl('hero_title', ...)`/`$bl('hero_lead', ...)`. Los 5 campos de imagen del admin (`img_hero`/`img_collage_1..4`) **no** se cablearon: el mockup aprobado `lat-07-contacto.jpeg` confirma un hero deliberadamente plano sin imagen — ver `docs/qa/contacto.md` y `docs/qa/BACKLOG-CONTENIDO.md` #12. | `tests/Feature/PageBlocksCmsSyncTest.php` (`test_contact_page_renders_cms_hero_blocks_when_present`, `test_contact_page_falls_back_to_default_copy_when_blocks_empty`) + evidencia de navegador (Playwright contra `:8002`/`lima_america_qa`): Page `QA_` con los 3 campos ES → aparecen en `/es/contacto`; purgada, vuelve al fallback. |
| 16 | `qa/paginas` | Hallazgo #4 (`panel-filament.md`): la sección "Misión, visión y valores" de `about.blade.php` ignoraba el repeater real `blocks.stats` (Misión/Visión/Valores/Equipo) que el admin expone y guarda, usando siempre el arreglo fijo `$defaultMvv` (o el campo huérfano `$b['mvv']`, sin UI en el admin). | `$mvvItems = $b['mvv'] ?? $defaultMvv;` nunca leía `$b['stats']`, el único campo de ese grupo con UI real en `PageResource`. | Se reemplazó por lógica que prioriza `$b['stats']` (mapea `title_es/en/pt`+`desc_es/en/pt`, asigna icono `target`/`eye`/`heart` por posición ya que el repeater no tiene campo de icono), cae a `$b['mvv']` (legado) y por último a `$defaultMvv`. El resto del formulario "Nosotros — contenido" (~20 campos de texto + 6 imágenes: banner CTA, tabs de pilares, grid de fotos, testimonios propios) **no** se cableó: el mockup aprobado `lat-02-nosotros.jpeg` (calcado en `_lat-about.scss`) no contempla esas secciones — ver `docs/qa/nosotros.md` y `docs/qa/BACKLOG-CONTENIDO.md` #13. | `tests/Feature/PageBlocksCmsSyncTest.php` (`test_about_page_renders_cms_stats_repeater_in_mvv_section`, `test_about_page_falls_back_to_default_mvv_when_stats_empty`) + evidencia de navegador: Page `QA_` con 3 items de `stats` (Misión/Visión/Valores) → aparecen en `/es/nosotros` con icono correcto por posición; purgada, vuelve al fallback (3 tarjetas fijas con tags de Valores). |
| 17 | `qa/paginas` | Hallazgos críticos C-1/C-2 (`docs/qa/seguridad-consolidada.md`): (a) endpoint `GET /_diag/mail` en `routes/web.php` protegido solo por token hardcodeado commiteado (`lvt-mail-diag-2026`), permitía enviar correo arbitrario con el SMTP del cliente y filtraba su config; (b) `composer audit` reportaba 38 advisories/17 paquetes, el más grave `livewire/livewire` 3.6.3 → **CVE-2025-54068 (RCE crítico)**, corregido en 3.6.4. | (a) Comentario propio decía "QUITAR después de resolver el problema de envío de reservas" y nunca se quitó. (b) Cadena de dependencias (Livewire/Filament/Symfony/Guzzle/commonmark) atrasada varios patches dentro del mismo major. | (a) Se eliminó la ruta `/_diag/mail` y su closure completos de `routes/web.php` (no queda ninguna referencia). (b) `composer update livewire/livewire symfony/mime symfony/http-foundation guzzlehttp/guzzle league/commonmark --with-dependencies` (Livewire 3.6.3→3.8.2, symfony/mime 6.4.21→6.4.41, symfony/http-foundation 6.4.22→6.4.42, guzzle 7.9.3→7.15.1, commonmark 2.7.0→2.8.3, + transitivos symfony/http-kernel, console, string, guzzlehttp/psr7, promises) — resuelve el RCE. Además `composer update "filament/*"` (3.3.26→3.3.54, dentro del constraint `^3.2` ya existente en `composer.json`, sin editarlo) — resuelve XSS de RichEditor (CVE-2026-55409), subida de archivo sin auth (CVE-2026-48500) y bypass de scope en Attach/Associate (CVE-2026-48067). `composer audit` bajó de 38 advisories/17 paquetes a **11 advisories/6 paquetes**, todos ligados a `laravel/framework` 10.x EOL y sus dependencias `symfony/mailer`\|`process`\|`routing`\|`yaml` (requieren Laravel 11/12, fuera de alcance — ver A-1) más `phpseclib/phpseclib` (transitivo de `culqi/culqi-php`, no tocado por estar en el árbol de la pasarela de pago, fuera del alcance de este fix). No se tocó `composer.json` (todo dentro de constraints existentes). | (a) `tests/Feature/DiagMailEndpointRemovedTest.php` (2 tests): con la ruta presente, `GET /_diag/mail?key=lvt-mail-diag-2026` devolvía 200 (confirmado antes del fix); tras eliminarla, ambos (con y sin token) devuelven 404. (b) `php artisan test` completo antes y después del update: sin cambios (4 failed baseline de `CheckoutTest`, resto passed: 110→112 tras sumar los 2 tests nuevos de (a)); `SmokeTest` verde; `php artisan filament:clear-cached-components` + `curl` a `:8002/admin` (200 tras redirect), `:8002/es` (200, home con `<title>` correcto), `:8002/es/tours`, `:8002/es/blog` (200) confirmando que Livewire/Filament siguen sirviendo el panel y el front. |
