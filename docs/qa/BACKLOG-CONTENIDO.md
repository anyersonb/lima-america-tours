# Backlog de contenido (🔵) — Lima América Tours

Hallazgos de **contenido/decisión del cliente**, NO defectos de código. Ningún agente los inventa ni los "tapa".
Son decisión del jefe. Origen: validación ad-hoc CRO + QA experto (2026-07-24/25).

| # | Módulo | Ítem 🔵 | Detalle | Decisión / responsable |
|---|---|---|---|---|
| 1 | Ficha de tour | Fotos equivocadas | El tour "Machu Picchu Full Day + Tren Panorámico desde Cusco" muestra fotos del litoral de Miraflores | Cargar fotos reales de Machu Picchu desde el admin |
| 2 | Ficha de tour | Itinerarios vacíos | Varias fichas muestran "El itinerario detallado estará disponible próximamente" | Cargar itinerario real por tour |
| 3 | Ficha de tour | Comparativa desactivada | El bloque "convencional vs premium" existe en el editor pero está off en todos los tours | Decidir si se activa (y en cuáles) |
| 4 | Ficha de tour | FAQ de prueba | Se agregaron FAQs de prueba (ES/EN/PT) al tour `huacachina-paracas-full-day` solo para validar el acordeón | Editar/limpiar y cargar FAQs reales |
| 5 | Contacto / global | Datos de contacto heredados del fork | **Teléfono/WhatsApp: CORREGIDO 2026-07-28.** Los Settings guardaban `+51 925 886 725` / `51925886725` — el número de **Lima View Tours, otro cliente** — y el FAB de WhatsApp del sitio mandaba los leads ahí. El código ya no tenía fallback (ver `ContactPhoneNoFallbackTest`), pero el valor seguía **en la BD**: el fix de código no toca datos ya guardados, y el cierre de QA lo había "revertido a su valor original" sin notar de quién era ese número (`panel-filament.md` §Settings, `F7-personas.md` §limpieza). Se cargó el real, verificado contra el sitio del cliente (limaamericatours.com): `+51 957 299 438` / `51957299438`. Barrido de la BD completa: 0 ocurrencias del ajeno. **Sigue pendiente el resto:** dirección `Av. Larcomar 233, Of. 410 — Miraflores, Lima` es idéntica a la de Lima View (heredada, sin confirmar); `contact_email` = `hola@limaamericatours.com` no existe en el sitio real, que publica `americatours09@gmail.com` e `infolimaamericatours@gmail.com`; horarios y handles de redes también vienen del seeder. | Confirmar con el cliente dirección, correo público, horarios y redes, y cargarlos en Configuración → Contacto. **Ningún dato de contacto se inventa ni se hereda:** si no hay dato, el Setting va vacío y el bloque no se pinta. Vale para cualquier entorno nuevo — sembrar una BD desde el fork vuelve a traer el número ajeno. |
| 6 | Home | Secciones del mockup | El Home real omite cifras/galería/CTA final del mockup y tiene un bloque newsletter no previsto | Decidir: implementar las secciones o actualizar el mockup de referencia |
| 7 | Global (higiene) | Datos de prueba en BD | Reservas `LVT-*`, carrito/lead `cro.test@example.com` ("CRO O'Test"), ContactLead "Carlos Prueba" | Purgar antes de producción |
| 8 | Reseñas | Google/TripAdvisor | Las tarjetas muestran 5.0 fijo; no se confirmó si están conectadas a API real o son ejemplo | Definir fuente real o quitar |
| 9 | Repo (interno) | Contexto heredado | `deploy-*.sh` y `CLAUDE.md` aún referencian infra/contexto de Lima View | Reescribir cuando se defina hosting de Lima América |
| 10 | Home | ~~Ofertas sin consumidor~~ (RESUELTO 2026-07-25) | Hallazgo #2 de `panel-filament.md`: `OfferResource` no se veía en ningún lado del front. Evaluado: `HomeController::fetchOffers()` **ya** calculaba `$offers` (activas, orden manual, límite 3) y se lo pasaba a `home.blade.php` sin usarlo — cablear la sección era trivial (sin diseño nuevo, solo reusar el patrón visual de "Tours Destacados"). Se implementó una sección "Ofertas especiales" en `home.blade.php` con datos reales del modelo. | **No requiere decisión del jefe** — ya está wireado (`qa/panel-filament`). El contenido real de cada oferta (título/imagen/precio/vigencia) sigue siendo responsabilidad del editor vía admin → Marketing → Ofertas. |
| 11 | Ficha de tour | Formulario de reseña del visitante | Al corregir el hallazgo #3 de `ficha-tour.md` (F2) se agregó la sección "Reseñas de viajeros" que **lee** `$tourReviews` (testimonios aprobados) en la ficha pública. La ruta `POST tours.review.store` (`TourController@storeReview`) ya existe en el backend y deja la reseña en `is_active=false` pendiente de moderación en Filament → Testimonios, pero **no hay ningún formulario en `show.blade.php` que apunte a esa ruta** — un visitante no puede escribir una reseña propia desde la ficha hoy. | Decidir si v1 lanza solo con reseñas de lectura (curadas por el admin vía Filament) o si se habilita también el formulario de envío público en esta misma ficha (con su UI, honeypot ya soportado por el backend, y el mensaje "pendiente de moderación" que el controlador ya devuelve). No se construyó proactivamente por ser una decisión de producto (exponer un formulario público de contenido moderable), no un defecto de código. |
| 12 | Contacto | 5 campos de imagen sin sección de diseño | `PageResource` (tab "Contenido de la página" → Contacto) expone `img_hero`+`img_collage_1..4`, pero el mockup aprobado (`docs/propuesta/exports/lat-07-contacto.jpeg`) confirma un hero deliberadamente **plano** (el propio código lo documenta: "HERO plano (sin imagen)"), sin imagen ni collage en ningún punto de la página. Se cablearon los 9 campos de texto del hero (eyebrow/título/lead ×3 idiomas, ver `docs/qa/contacto.md`), pero las 5 imágenes quedan sin consumidor a propósito — construir una sección nueva sin mockup sería inventar diseño. | Decidir entre (a) encargar el diseño de un hero con imagen/collage para Contacto, o (b) eliminar esos 5 campos de `PageResource` (recorte de formulario, tarea de `backend-laravel`). |
| 13 | Nosotros | ~20 campos + 6 imágenes sin sección de diseño | `PageResource` (grupo "Nosotros — contenido") expone `why_intro`/`why_intro2`/`hero_cta_label`/`why_cta_label`/`banner_heading`/`banner_text`/`cultura_heading`/`cultura_intro`/repeater `pillars`/`testimonios_eyebrow`/`testimonios_heading` + `img_grid1..4`/`img_banner_cta`/`img_testimonios`. El mockup aprobado (`docs/propuesta/exports/lat-02-nosotros.jpeg`, calcado 1:1 en `_lat-about.scss`) solo contempla Hero → Split "10 años" → Misión/Visión/Valores → Stats band → CTA final — ninguna de esas ~26 entradas del formulario tiene sección viva. Se cableó el repeater `stats` (Misión/Visión/Valores/Equipo) reusando el slot MVV ya existente (mismo diseño, ver `docs/qa/nosotros.md`), pero el resto queda sin implementar a propósito. Además, en sentido inverso, `about.blade.php` lee `$b['destinations']`/`$b['split_*']`, campos que **no existen** en `PageResource` (huérfanos sin UI de admin). | Decidir entre (a) encargar una fase 2 de diseño para banner CTA/tabs de pilares/grid de fotos/testimonios propios, o (b) recortar `PageResource` a los campos que sí tienen slot. Para los huérfanos inversos: agregar los campos faltantes a `PageResource` (tarea de `backend-laravel`) si se quiere que el editor controle esos textos/imagen desde el admin. |
